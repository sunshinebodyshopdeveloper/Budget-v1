<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/google_background_settings"
    android:fillViewport="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">
        <!-- Toolbar tipo Google / Settings -->
        <com.google.android.material.appbar.MaterialToolbar
            android:id="@+id/toolbar"
            style="@style/Widget.Material3.Toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/google_background_settings"
            app:title="@string/account_profile_title_1"
            app:titleCentered="false"
            app:navigationIcon="@drawable/ic_arrow_back"
            app:navigationIconTint="@color/google_icons"
            app:titleTextColor="@color/google_icons_state" />

        <!-- Subtítulo / descripción -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="24dp"
            android:background="@drawable/bg_bottom_sheet">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                style="@style/Text.AppSuite.SectionTitle"
                android:text="@string/account_profile_subtitle"
                android:textColor="@color/google_icons_state" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                style="@style/Text.AppSuite.Body"
                android:text="@string/account_profile_desc"
                android:textColor="@color/google_dark" />

            <!-- Chip de visibilidad -->
            <com.google.android.material.chip.Chip
                style="@style/Widget.Material3.Chip.Assist"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:clickable="false"
                android:focusable="false"
                android:text="@string/account_profile_notice"
                app:chipBackgroundColor="@color/menu_chip_bg_selected"
                app:chipCornerRadius="100dp"
                app:chipStrokeWidth="0dp" />
        </LinearLayout>

        <!-- Centro: icono / foto -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:gravity="center"
            android:orientation="vertical"
            android:background="@color/google_white">

            <com.google.android.material.imageview.ShapeableImageView
                android:id="@+id/ivAvatar"
                android:layout_width="250dp"
                android:layout_height="250dp"
                android:contentDescription="@string/account_profile_title_1"
                android:scaleType="centerCrop"
                android:adjustViewBounds="false"
                app:shapeAppearance="@style/ShapeAppearance.Material3.Corner.Full"
                app:strokeWidth="0dp"
                android:src="@drawable/ic_avatar" />

            <com.google.android.material.card.MaterialCardView
                android:id="@+id/btnEditProfilePhoto"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:layout_marginTop="-68dp"
                android:layout_marginStart="85dp"
                android:clickable="true"
                android:focusable="true"
                android:foreground="?attr/selectableItemBackgroundBorderless"
                app:cardBackgroundColor="@color/menu_chip_bg_selected"
                android:contentDescription="@string/work_order_detail_os"
                app:cardCornerRadius="28dp"
                app:cardElevation="0dp"
                app:cardUseCompatPadding="false"
                app:strokeWidth="0dp">

                <ImageView
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    android:layout_gravity="center"
                    android:contentDescription="@string/work_order_detail_os"
                    android:src="@drawable/ic_camera"
                    app:tint="@color/menu_button_icon" />
            </com.google.android.material.card.MaterialCardView>
        </LinearLayout>

        <com.google.android.material.button.MaterialSplitButton
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center">
            <Button
                android:id="@+id/btnChangePhoto"
                style="?attr/materialSplitButtonLeadingFilledStyle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/account_profile_btn_save_photo"
                app:icon="@drawable/ic_edit"
                app:backgroundTint="@color/google_blue"/>
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnRemovePhoto"
                style="?attr/materialSplitButtonLeadingFilledStyle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:contentDescription="@string/account_profile_btn_remove_photo"
                app:icon="@drawable/ic_delete"
                app:backgroundTint="?attr/colorErrorContainer"
                app:iconTint="?attr/colorOnErrorContainer"
                app:rippleColor="?attr/colorError" />
        </com.google.android.material.button.MaterialSplitButton>
    </LinearLayout>
</ScrollView>
